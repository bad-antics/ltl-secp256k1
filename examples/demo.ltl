// examples/demo.ltl — runnable demo for ltl_secp256k1

import std::io
use ltl_secp256k1 as lib

fn main() {
    io::println("ltl_secp256k1 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}